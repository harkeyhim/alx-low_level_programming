#include "main.h"

/*
 * _islower - Entry point
 *
 *Description:  A functions that checks for lowercase characters
 *
 * Return : Always 0(success)
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
